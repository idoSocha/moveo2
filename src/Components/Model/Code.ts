class Code {
  public id: number;
  public title: string;
  public code: string;
  constructor(id: number, title: string, code: string) {
    this.id = id;
    this.title = title;
    this.code = code;
  }
}
export default Code;
